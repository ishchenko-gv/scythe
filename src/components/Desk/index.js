import React from 'react';

import GameBoard from '../GameBoard';
import PlayerTablet from '../PlayerTablet';

const Desk = () => {
  return (
    <>
      <GameBoard />
      <PlayerTablet />
    </>
  );
};

export default Desk;
